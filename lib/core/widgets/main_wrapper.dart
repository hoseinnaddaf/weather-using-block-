import 'package:flutter/material.dart';

class MainWrapperer extends StatelessWidget {
  const MainWrapperer({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Container(),
    );
  }
}
